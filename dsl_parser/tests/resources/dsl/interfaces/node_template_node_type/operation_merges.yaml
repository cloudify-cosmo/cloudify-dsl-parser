node_types:
    cloudify.types.base:
        interfaces:
            cloudify.interfaces.lifecycle:
                start: mock.tasks.start

node_templates:
    base:
        type: cloudify.types.base
        interfaces:
            cloudify.interfaces.lifecycle:
                create: mock.tasks.create
