node_types:
    cloudify.types.base:
        interfaces:
            cloudify.interfaces.lifecycle:
                create: {}
                start:
                    implementation: mock.tasks.start
                    inputs:
                        key:
                            default: value
                stop: mock.tasks.stop


node_templates:
    base:
        type: cloudify.types.base
